#include <stdio.h>
/*
	文件包含：
		要用库函数，需要库函数的声明
	格式：
	#include<文件名>
		只会在系统文件中查找文件名
	#include"文件名"
		在当前项目文件夹中查找，如果找不到再去系统文件中查找
	文件允许嵌套包含：在一个被包含文件中可以包含其他文件
	不能重复包含，.h 文件中  #pragma once防止重复包含 VS中有
*/


int main(){
	
	
	return 0;
}
